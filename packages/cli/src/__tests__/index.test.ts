// TODO: Actually have some tests.
// This file just exists to let everything pass for now

let done: void;
describe("writing the tests", () => {
  it("should have some tests", () => {
    expect(it).not.toBe(done);
    expect(it).not.resolves;
  });
});
